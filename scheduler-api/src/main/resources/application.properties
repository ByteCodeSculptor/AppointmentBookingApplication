spring.application.name=scheduler-api

# --- Database Connection ---
# This says: "Use the Env Variable if it exists (Render), otherwise use localhost (Laptop)"
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/AppointmentBooking}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:Vishnu@289}

# --- JPA / Hibernate ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# --- Port Configuration ---
server.port=${PORT:8080}